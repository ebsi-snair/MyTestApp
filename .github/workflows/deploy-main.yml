name: Deploy to PROD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  branch: "main"

jobs:
  build-workflow:
    uses: ./.github/workflows/build-workflow.yml
    with:
      branch: $branch
