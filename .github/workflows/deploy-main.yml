name: Deploy to PROD

env:
  branch: "main"

on:
  push:
    branches: [ $branch ]
  pull_request:
    branches: [ $branch ]

jobs:
  build-workflow:
    uses: ./.github/workflows/build-workflow.yml
    with:
      branch: $branch
